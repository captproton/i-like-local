<h1>New city</h1>

<%= error_messages_for :city %>

<% form_for(:city, :url => state_cities_path(@state)) do |f| %>
<%= render :partial => 'form', :locals => {:f => f} %>

  <p>
    <%= submit_tag "Create" %>
  </p>
<% end %>

<%= link_to 'Back', cities_path %>